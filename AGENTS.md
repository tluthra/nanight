# Agent Instructions

- Make the minimal changes needed to satisfy the request.
- Avoid unrelated refactors, formatting churn, or broad rewrites.
- After making changes, run the relevant compile or build command when a buildable target exists.
- Fix all compilation errors before handing off.
- If compilation cannot run because of signing, sandboxing, or environment limits, report the exact reason and the closest verification command that did run.
